rules_version = '2';

// Ogni utente legge e scrive soltanto il proprio calendario.
service cloud.firestore {
  match /databases/{database}/documents {
    match /calendars/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
